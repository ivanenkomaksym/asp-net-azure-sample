class User {
    constructor(firstName, lastName, email, picture) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.email = email;
        this.picture = picture;
    }
}

// Export the User class
module.exports = User;